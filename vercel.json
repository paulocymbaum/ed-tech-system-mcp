{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "src/mcp_server/vercel_app.py": {
      "maxDuration": 60,
      "memory": 1024,
      "excludeFiles": "{tests/**,ui/**,changelog/**,.cache/**,.chroma/**,chroma_data/**,docs/**,backlog/**,fixtures/**,test-dataset/**}"
    }
  }
}
